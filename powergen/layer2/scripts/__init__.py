# Layer 2 scripts
#
# validate.py      — verify schema locators resolve against source .pptx (SKILL.md Step 6)
#                    Usage: python -m powergen.layer2.scripts.validate <path/to/template.schema.json>
# inspect_pptx.py  — dump shape inventory from a .pptx (SKILL.md Step 1, not yet implemented)
