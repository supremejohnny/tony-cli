"""Removed: replaced by spec.py (Call 1) + compiler.py (Call 2)."""
raise ImportError(
    "powergen.layer2.composer.planner is removed. "
    "Use spec.generate_spec + compiler.compile_plan instead."
)
